# VOC 티켓 알림 업무기준 v1

| 항목 | 값 |
|---|---|
| 적용 대상 | 플러그링크커넥트 VOC 티켓 → Slack `#s3-사업개발-voc-tickets` 알림 자동화 |
| 구성 | **1부** 알림 산출물 품질 · **2부** 변경·배포 작업 기준 |
| 판정 주체 | 1차 김우중(쿠이) |
| 작성 | 2026-07-16 · 개정 시 새 버전(v2)으로 |
| 근거 문서 | `AUDIT.md`(검수), `MIGRATION_GITHUB_ACTIONS.md`(배포), `SETUP_GUIDE.md`(로컬) |

## 한 장 요약 (네 칸)

| 정확성 | 형식 | 근거 | 안전 |
|---|---|---|---|
| 배분된 티켓은 **빠짐없이 1번만**. 중간상태를 완료로 알리지 않는다. 0건과 조회실패를 구분한다. | 신규 알림·완료 스레드·일일 요약 3종. 담당자는 **실제 Slack 멤버 ID 멘션**. | 값의 SoT는 **커넥트 API 필드**. state 파일은 캐시일 뿐. | 시크릿은 소스에 없다. config/state/log는 커밋 금지. 발송 전 상태 커밋 금지. |

---

# 1부. 알림 산출물 품질 기준

## 1. 정확성 — 무엇이 맞는 답인가

### 공통 판정 원칙

| # | 기준 | 이유 |
|---|---|---|
| 1 | **신규 판정** = 담당자별 티켓ID 스냅샷 대비 새로 등장 **AND** `status == RECEIVED` **AND** `assignedAdminUserName` 존재 | 배분 시각 필드를 신뢰하지 않고 스냅샷 diff를 기준으로 삼는다 |
| 2 | **첫 실행은 발송 0건** (스냅샷만 초기화) | 최초 가동에 누적분 대량 발송 방지 |
| 3 | **0건 ≠ 데이터 없음**. 이전 스냅샷이 있는데 조회가 0건이면 **비정상 응답으로 간주**하고 스냅샷을 갱신하지 않는다 | 빈 응답이 스냅샷을 리셋하면 그 사이 티켓이 알림 없이 흡수됨 |
| 4 | **1건당 1번만**. 발송 **성공 후에만** 발송 완료로 기록. 실패는 재시도 큐로 남긴다 | 발송 전 기록 → 실패 시 영구 유실 / 기록 누락 → 중복 발송 |
| 5 | **완료 판정은 화이트리스트**: `COMPLETED`, `CANCELLED`만 종결. `PROCESSING`·`MONITORING`은 완료가 아니며 계속 추적한다 | 실측 status enum 기준. 중간상태를 완료로 알리면 담당자가 종결로 오인 |
| 6 | **초소(CANCELLED)는 종결로 집계**한다 (팀 결정) | 더 이상 조치가 필요 없는 상태이므로 완료와 함께 센다 |
| 7 | 요약의 **완료일자는 `completedAt`, 없으면 `updatedAt`으로 폴백** | 실측상 COMPLETED의 약 절반이 `completedAt` 미기입 → 폴백 없으면 대량 누락 |
| 8 | 담당자 표기는 **실제 Slack 멤버 ID 멘션**(`<@U…>`)이어야 한다. 평문 `@닉네임`은 **실패**로 본다 | 평문은 메시지는 보이지만 당사자에게 알림이 가지 않는다(무음 실패) |

### 정량 기준

| 지표 | 기준 |
|---|---|
| 미발송(누락) | **0건** — 배분됐는데 알림이 없으면 ERROR |
| 중복 발송 | **0건** |
| 알림 지연(SLA) | **배분 후 20분 내 도달** (10분 주기 + 실행시간 여유). **전제: n8n이 10분마다 `workflow_dispatch`를 호출**(`N8N_TRIGGER.md`) — Actions 내장 cron만으로는 달성 불가(실측 하루 3~4회) |
| 완료 오탐 | **0건** — 중간상태를 완료로 알리면 ERROR |
| 일일 요약 | 평일 09:00 발송, 미발송이면 ERROR |

### 알려진 한계 (기준 위반 아님, 인지 사항)

- 담당자 간 **재배분은 재알림되지 않는다** (알림 키가 티켓 단위). 재배분 통보가 필요해지면 별도 요건으로 다룬다.
- **GitHub Actions 내장 cron은 신뢰하지 않는다.** 2026-09 실측: 설정은 평일 ≈54회/일(10분마다)인데 실제 실행은 **하루 3~4회**(업무시간 내 2~3회), 최대 지연 약 4시간 50분, 일일 요약도 11~12시에 발송됨. 실행된 건은 100% 성공이라 겉보기엔 정상인 **무음 지연**이므로, 초록불만 보고 정상으로 판단하지 말 것.
- 따라서 SLA 판정은 **n8n 트리거가 활성 상태임을 전제**로 한다. n8n이 멈추면 알림은 조용히 하루 3~4회로 떨어진다 → 주기 점검 대상(`N8N_TRIGGER.md` 운영 메모).

## 2. 형식 — 어떤 모양이어야 하는가

| 산출물 | 형식 |
|---|---|
| **신규 티켓 알림** | header `🎫 새 티켓 배분 알림 (#티켓ID)` → 필드 4개(생성시각 / 담당자 멘션 / 충전소 / 주소) → `📝 요약`(본문 앞 3줄 병합, 최대 150자 절단) → `티켓 확인하기` 버튼 → divider |
| **완료 스레드** | 원본 알림 **메시지의 스레드에 댓글**로. `✅ 티켓 #ID {상태라벨}` + 담당자 멘션 + 처리시각. 라벨: COMPLETED=처리 완료, CANCELLED=취소 |
| **일일 요약** | header `📋 VOC 티켓 일일 현황 — YYYY년 MM월 DD일` → **현재 잔여**(전체 + 팀별 + 담당자별 건수) → **전일 변화**(팀별 신규 배분 / 처리 완료 목록, 티켓 링크 포함) → 전체 합계 |
| 공통 | 채널은 `#s3-사업개발-voc-tickets` 고정. 팀 구분은 영업관리팀 / 시공관리팀. 섹션 텍스트 2,900자 초과 시 절단 |

## 3. 근거 — 어디서 나온 값인가

**원칙: 티켓 상태의 SoT는 항상 커넥트 API.** state 파일은 "지난번에 무엇을 봤는가"를 기억하는 캐시일 뿐이며, 손상 시 폐기·재초기화해도 무방하다(단 재초기화 구간의 티켓은 알림 없이 흡수됨을 인지).

| 알림에 쓰는 값 | 출처 |
|---|---|
| 담당자 | API `assignedAdminUserName` — **시트가 아니라 API가 기준** |
| 담당자 멘션 ID | `ticket_config.json` → `slack_member_ids` (닉네임 부분일치) |
| 충전소명 / 주소 | `targetStationName` / `targetStationAddress` |
| 요청 본문 | `requestContent` |
| 생성시각 | `createdAt` — **KST-naive 문자열**(실측 확인). 타임존 변환 불필요 |
| 완료시각 | `completedAt` → 없으면 `updatedAt` |
| 상태 | `status` (RECEIVED / PROCESSING / MONITORING / COMPLETED / CANCELLED) |
| 티켓 링크 | `parentVocId` → `/operation/vocs/manage/{parentVocId}?ticketId={id}` |
| 신규 판정 기준선 | state `employee_snapshot` |
| 중복 방지 | state `notified_ids` / 스레드 매핑 `ticket_ts_map` / 완료 이력 `completed_ids` |

## 4. 안전 — 무엇을 쓰면 안 되는가

| 금지 | 내용 |
|---|---|
| 시크릿 소스 하드코딩 | 비밀번호·토큰은 소스에 **절대** 두지 않는다. `ticket_config.json`(gitignore) 또는 환경변수/Actions Secrets만 |
| 커밋 금지 파일 | `ticket_config.json`, `ticket_state.json`, `*.log` |
| 노출 금지 | 토큰·비밀번호를 로그·커밋 메시지·Slack·이슈에 남기지 않는다 |
| 발송 전 상태 커밋 | "알림함" 기록을 발송 성공 확인 **전에** 남기지 않는다 |
| 완료 오탐 | 중간상태(PROCESSING·MONITORING)를 완료로 알리지 않는다 |
| 채널 확대 | 본문·주소를 싣는 현행 유지는 **내부 채널 한정** 전제. 채널 공개 전환·외부 연동·타 채널 전달은 별도 승인 필요 |
| 임의 재초기화 | state 삭제는 그 구간 티켓을 무음 흡수시킨다. 필요 시 사유·시점을 남긴다 |
| 승인 없는 배포 | commit/push는 승인 후. force-push 금지 |

> **잔여 리스크(인지 후 수용)**: 현재 로그인은 개인 admin 계정을 사용한다. 유출 시 조회 범위가 팀 경계를 넘는다. 전용 서비스 계정 전환이 권장 사항으로 남아 있다(미이행).

## 5. 검증 — 무엇을 보고 정상이라 하는가 *(추가 칸)*

| 시점 | 확인 |
|---|---|
| 코드 변경 시 | `--dry-run`으로 **Slack 미발송·state 미변경** 확인 → 이후 실제 1회 |
| 배포 후 | Actions run 초록불 + 로그에 `Cache restored from key: …`(스냅샷 복원됨) |
| 표본 점검 | 알림 1건을 골라 커넥트에서 열고 담당자·상태·충전소가 일치하는지 대조 |
| 실패 감지 | 실행 실패 시 Slack 경보(`VOC_ALERT_TARGET`) 수신. 경보도 알림도 없이 조용하면 **Actions 실행 이력**을 확인(무음 중단 의심) |
| 주간 | 최근 실행에 실패가 있는지 · 잔여 티켓 수가 비정상(0 또는 과다)인지 |

## 6. 판정 등급

| 등급 | 해당 |
|---|---|
| **ERROR** (즉시 조치) | 미발송·중복 발송 · 중간상태를 완료로 오알림 · 일일 요약 미발송 · 시크릿 노출 · 승인 없는 push |
| **WARNING** (당일 확인) | SLA 20분 초과 · 평문 `@닉네임` 멘션(핑 안 감) · 요약 완료 0건이 의심스러울 때 · 실패 경보 미설정 |
| **PASS** | 위 항목 없음 |

---

# 2부. 변경·배포 작업 기준

## 1. 착수 전

- 지시서(handoff) 또는 요청의 **범위를 먼저 확정**한다. 범위 밖(cron 주기, 시크릿명, 다른 스텝, 알림 포맷)은 임의로 바꾸지 않는다.
- 지시 내용과 실제 코드가 어긋나면 **중단하고 보고**한다. 표면 오류 뒤에 2차 실패가 숨어 있을 수 있다(예: 파일명 불일치를 고치면 다음 스텝에서 다른 이유로 실패).

## 2. 변경 중

| 순서 | 항목 |
|---|---|
| 1 | 최소 변경 — `git diff`로 의도한 곳만 바뀌었는지 확인 |
| 2 | 정적 검증 — 워크플로우는 YAML 파싱, 스크립트는 컴파일 |
| 3 | **드라이런 우선** — `--dry-run`으로 부수효과 없이 출력만 확인 |
| 4 | 실제 1회 실행 후 결과 대조 |

## 3. 배포

- **승인 게이트**: commit·push는 판정 주체(쿠이) 승인 후에만. force-push 금지.
- **시크릿 입력은 사람이 한다**: 에이전트는 Secret 값을 입력하지 않는다. 등록 시 **개행 혼입 주의**(`gh secret set -b "값"` 또는 `printf '%s' | gh secret set`) — 개행이 섞이면 "계정 없음"류로 엉뚱하게 실패한다.
- 이중 실행 금지: 새 실행 경로를 켜면 **기존 경로를 반드시 끈다**(로컬 Task ↔ Actions 동시 가동 금지).
- 배포 후 `workflow_dispatch` 1회로 초록불 확인.

## 4. 사후

- **롤백 경로를 남긴다**: 로컬 방식 복구 `Enable-ScheduledTask`, 클라우드 중단 `gh workflow disable`.
- 스코프 밖에서 발견한 개선점은 **별도 태스크로 분리**하고 이번 변경에 섞지 않는다.
- 완료 즉시 Notion 일일 페이지에 성과 기록. 재사용 지식은 `knowledge-raw`에 노트로 남긴다.

---

## 부록. 이 기준의 근거가 된 실측 사실

| 사실 | 확인 방법 |
|---|---|
| status enum = RECEIVED / PROCESSING / MONITORING / COMPLETED / CANCELLED | API 표본 조회(담당자 8명 × 최대 300건) |
| COMPLETED의 약 절반이 `completedAt` 미기입 | 동일 표본에서 날짜필드 존재 여부 집계 |
| `createdAt` 등은 KST-naive 문자열(오프셋 없음) | 라이브 샘플링 — 타임존 어긋남 버그는 성립하지 않음 |
| 빈 응답이 스냅샷을 리셋하면 티켓이 무음 흡수됨 | 코드 경로 검증(6차원 검수 + 적대적 검증) |
| 개인 PC 방식은 실제로 영업일 무음 중단이 발생했음 | 실행 로그 커버리지 공백 확인 |
