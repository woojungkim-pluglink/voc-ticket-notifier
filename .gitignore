# 시크릿·상태·로그는 절대 커밋/공유하지 않는다
ticket_config.json
ticket_state.json
*.log

# 파이썬 캐시
__pycache__/
*.pyc

# 배포·공유 대상은 코드 + *.example.json 뿐
!ticket_config.example.json

# 플링비즈 일일 현황: 매 실행마다 새로 생성되는 조회결과·감사로그
es_input.json
flingbiz_runs.jsonl

# 상태파일 백업(운영 데이터) — 커밋 금지
*.bak-*
*.bak
