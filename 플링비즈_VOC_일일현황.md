# 플링비즈 VOC 일일 현황 — 운영 문서

- 작성 2026-09-08 · 담당 김우중(쿠이)
- 요청: 플링비즈 팀 — "자산소유주 기준 플링비즈 VOC만 별도 알림"
- 데이터 등급: 🟡 내부

## 한 줄 요약

평일 09:00, 자산소유주 7종(203개소)의 **미완료 VOC 티켓**을 소유주별로 집계해 `#플링비즈알람용`에 게시한다. VOC 접수일부터 **달력일 7일** 이상 경과한 건은 별도 섹션에 나열한다.

**기존 VOC 티켓 알림 봇(담당자 배분 기준, `#쿠이슬랙비서`)은 전혀 건드리지 않았다.** 코드·설정·상태파일·워크플로우 모두 분리되어 있다.

## 왜 별도 파이프라인인가

기존 봇은 **담당자 배분(`assignedAdminUserName`)** 기준이라 소유주로 필터할 수 없다. 자산소유주는 커넥트 REST(`/crms/tickets`, `/crms/vocs`)에 **필드 자체가 없다** — 파트너 관련 필드는 `cpoName`·`assignedPartnerName`·`createdPartnerName`뿐이고 전수가 "플러그링크"(운영사)라 대상 소유주 7종과 0건 일치한다. 소유주는 ES `alias_chargers_production.owner.partnerName`에만 있다.

## 실행 구조

```
Claude Code 스케줄러 (평일 09:03 로컬)
  └─ ES MCP 조회 4건 → es_input.json 작성
       └─ flingbiz_daily.py (집계·판정·문구·게시)
            ├─ 커넥트 REST: parentVocId → VOC 접수일
            ├─ Slack chat.postMessage → #플링비즈알람용
            └─ flingbiz_runs.jsonl (감사 로그)
```

- 스케줄 작업: `C:\Users\user\.claude\scheduled-tasks\flingbiz-voc-daily\SKILL.md`
- **제약: Claude 앱이 열려 있을 때만 실행된다.** 닫혀 있으면 다음 실행 시 밀려서 1회 수행.
- ES 접근 권한을 개발팀에 별도 요청하지 않는 대신 이 제약을 받아들였다(아래 "한계" 참조).

### 역할 분담 (의도적)

| 주체 | 하는 일 |
|---|---|
| Claude(스케줄러) | ES 조회 결과를 `es_input.json`으로 **옮기기만** 한다 |
| `flingbiz_daily.py` | 집계·경과일 판정·문구 생성·게시·로깅 **전부** |

숫자와 문장을 LLM이 만들지 않게 분리한 것이다. 스케줄러 프롬프트에 "집계·판정·문구 생성을 직접 하지 말 것"을 명시해 두었다.

## 판정 기준

| 항목 | 기준 | 근거 |
|---|---|---|
| 대상 자산 | `flingbiz_config.json`의 `asset_owners` 7종 | 요청 팀 지정. **충전소 목록은 하드코딩하지 않고 매 실행 ES 조회** (신규 개시분 자동 반영) |
| 대상 티켓 | `parentVocId` 있는 티켓만 | 요청 팀 검증 기준과 동일 |
| 미완료 | status ∉ {COMPLETED, CANCELLED, CLOSED, DONE, CANCELED, FINISH, FINISHED} | 실측 enum은 RECEIVED/PROCESSING/MONITORING/COMPLETED/CANCELLED. 취소도 종결로 집계(요청 팀 합의) |
| 경과일 | **VOC 접수일**부터 **달력일** (영업일 아님) | 사용자 지정 2026-09-08 |
| 강조 임계 | 7일 | 요청 팀 지정 |
| 직전 구간 | 직전 영업일 09:00 KST ~ 오늘 09:00 KST | 월요일 실행 시 주말 누락 방지 |

경과일이 티켓 생성일이 아니라 **VOC 접수일** 기준이라 실제 지연이 더 길게 나온다. 예: #637298은 티켓 08-31이지만 VOC 접수 08-24 → 15일 경과.

## 검증 (2026-09-08 실측)

| 항목 | 요청 팀 기준값 | 재현 | 판정 |
|---|---|---|---|
| 자산 규모 | 203개소 · 776대 | 203개소 · 776대 | 일치 |
| 소유주별 개소 | — | 한백 74 / 스칼라 51 / Biz_P 34 / Biz_H 20 / 대한송유관 12 / 타키온 10 / 차지네틱스 2 | 합계 203 |
| 미완료 | 6건 | 7건 | 일치 (당일 11:39 신규 #641699 1건 차이) |

`station_production`(월 스냅샷)을 쓰면 최신이 2026-08-01이라 8월 이후 신규 충전소가 빠지고 **티켓 34건이 미탐지**된다. 그래서 실시간 인덱스 `alias_chargers_production`만 쓴다.

## 시간대 함정 (반드시 기억)

- ES `createdAt`·`completedAt` = **UTC naive** 문자열
- 커넥트 REST `createdAt` = **KST naive** 문자열
- 섞으면 9시간 밀린다. `flingbiz_daily.py`의 `es_utc_to_kst()` / `rest_kst()`가 정규화한다. **ES 값은 가공하지 말고 그대로 `es_input.json`에 옮긴다.**
- KST 09:00 = 같은 날 UTC 00:00 → 직전 구간 range 문자열은 날짜만 갈아끼우면 된다.

## 안전장치

| 상황 | 동작 |
|---|---|
| 소유주 매핑이 빈 결과 | 스크립트가 발송 중단(`sys.exit(1)`). 잘못된 "0건" 게시 방지 |
| ES 조회 실패·MCP 불가 | 스케줄러가 6단계를 실행하지 않고 원인만 보고 |
| 충전소 수 20% 이상 급변 | 스케줄러가 중단 후 보고(인덱스 이상 의심). 직전 실행값은 `flingbiz_runs.jsonl` |
| VOC 접수일 조회 실패 | 해당 건은 경과일 판정에서 제외하고 메시지에 "⚠️ 조회 실패 N건" 표기 |
| 쿼리에 쓴 소유주·종결상태가 config와 불일치 | 발송 중단(`exit 1`) — `es_input.json`의 `query_owners`·`query_completed_statuses`를 config와 대조 |
| Slack 게시 실패(`ok:false`) | 로그에 `slack_ts:null` 남기고 `exit 2` — 종료코드 0이면 미게시가 성공으로 기록됨 |
| 설정에 있으나 ES에 없는 소유주 | `[WARN]`으로 표기하고 계속(오탈자·계약 종료 조기 감지) |

## 바꾸는 방법

- **파트너 추가/제거**: `flingbiz_config.json`의 `asset_owners` 배열만 수정. 코드·충전소 목록 수정 불필요
- **임계일 변경**: `stale_threshold_days`
- **채널 변경**: `slack_channel_id`
- **실행 시각**: 스케줄 작업의 cron (`0 9 * * 1-5`)

## 한계 (요청 팀에 공유할 것)

1. **Claude 앱이 열려 있어야 실행된다.** 닫혀 있으면 그날 09:00 발송이 밀린다. 상시 보장이 필요해지면 ES 읽기 권한을 받아 GitHub Actions로 옮기면 된다(요청서 `플링비즈_ES접근요청.md` 보류 상태로 보관).
2. **실시간 알림은 미구현.** 1차는 일일 요약만. 요청 팀도 일일 요약을 우선순위로 지정.
3. **딜라이브는 1차 제외.** 요청 팀이 포함 여부 확인 중.
4. **전일 완료 집계는 `completedAt` 기준.** 결측률을 ES로 실측하면 전체 이력은 15.5%(209,129건 중 32,359건)이지만 **2026-07-01 이후 생성분은 0.03%**(31,692건 중 8건)다. 즉 일일 구간 집계에는 사실상 영향이 없고, 오래된 건을 소급 집계할 때만 주의. 미완료 현황(핵심 지표)은 status 기준이므로 무관.

## 인계 (2026-09-08 진행 중)

플링비즈 담당(메윤)에게 인계 중. 인계 문서: `HANDOFF_플링비즈VOC_구현안내.md` — 메윤이 자기 Claude Code에서 처음부터 세울 수 있게 쓴 자기완결 문서(전용 스크립트 전문·ES 쿼리·함정 9종·인수 완료 기준 포함).

**메윤 쪽이 같은 채널로 전환되는 시점에 이 작업(`flingbiz-voc-daily`)을 비활성해야 한다.** 안 하면 09:00과 09:03에 조회 시점·계정이 다른 두 숫자가 같은 채널에 올라간다. 전환 순서는 인계 문서 §3-0.

요청 팀에 아직 고지하지 않은 항목(전환 시 메윤이 고지): 정시 미보장, 완료 수치의 `completedAt` 근거, 실시간 알림 미구현.

## 관련 파일

| 파일 | 역할 |
|---|---|
| `flingbiz_daily.py` | 집계·판정·문구·게시 (결정적 로직 전부) |
| `flingbiz_config.json` | 소유주 목록·채널·임계값 |
| `es_input.json` | 매 실행 생성되는 ES 조회 결과 (gitignore) |
| `flingbiz_runs.jsonl` | 실행 감사 로그 (gitignore) |
| `플링비즈_ES접근요청.md` | 개발팀 권한 요청서 — **보류**(MCP 경로로 우회) |
| `QUALITY_STANDARD.md` | 기존 VOC 알림 업무 품질기준 |
